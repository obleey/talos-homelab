alloy:
  configMap:
    content: |
      prometheus.exporter.blackbox "probes" {
        config = `
      modules:
        http_home:
          prober: http
          timeout: 10s
          http:
            valid_status_codes: [200, 201, 301, 302, 401, 403, 404]
            method: GET
            follow_redirects: true
            tls_config:
              insecure_skip_verify: true
      `

        // Kubernetes cluster services
        target {
          name    = "https://argocd.talos-cluster.local.obleey.com"
          address = "https://argocd.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://bazarr.talos-cluster.local.obleey.com"
          address = "https://bazarr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://clonarr.talos-cluster.local.obleey.com"
          address = "https://clonarr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://grafana.talos-cluster.local.obleey.com"
          address = "https://grafana.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://hub.talos-cluster.local.obleey.com"
          address = "https://hub.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://jellyfin.talos-cluster.local.obleey.com"
          address = "https://jellyfin.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://kubeseal.talos-cluster.local.obleey.com"
          address = "https://kubeseal.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://longhorn.talos-cluster.local.obleey.com"
          address = "https://longhorn.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://plex.talos-cluster.local.obleey.com"
          address = "https://plex.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://prowlarr.talos-cluster.local.obleey.com"
          address = "https://prowlarr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://qb.talos-cluster.local.obleey.com"
          address = "https://qb.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://radarr.talos-cluster.local.obleey.com"
          address = "https://radarr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://seerr.talos-cluster.local.obleey.com"
          address = "https://seerr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://sonarr.talos-cluster.local.obleey.com"
          address = "https://sonarr.talos-cluster.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://traefik.talos-cluster.local.obleey.com"
          address = "https://traefik.talos-cluster.local.obleey.com"
          module  = "http_home"
        }

        // Home network appliances
        target {
          name    = "https://technitium.local.obleey.com"
          address = "https://technitium.local.obleey.com"
          module  = "http_home"
        }
        target {
          name    = "https://truenas.local.obleey.com"
          address = "https://truenas.local.obleey.com"
          module  = "http_home"
        }
      }

      prometheus.scrape "blackbox" {
        targets         = prometheus.exporter.blackbox.probes.targets
        forward_to      = [prometheus.relabel.blackbox_labels.receiver]
        job_name        = "blackbox"
        scrape_interval = "60s"
        scrape_timeout  = "15s"
      }

      // Fix labels: set instance = target URL, job = "blackbox"
      prometheus.relabel "blackbox_labels" {
        forward_to = [prometheus.remote_write.prometheus.receiver]

        rule {
          source_labels = ["job"]
          regex         = "integrations/blackbox/(.*)"
          target_label  = "instance"
          replacement   = "$1"
        }

        rule {
          target_label = "job"
          replacement  = "blackbox"
        }
      }

      prometheus.remote_write "prometheus" {
        endpoint {
          url = "http://kps-prometheus.monitoring.svc.cluster.local:9090/api/v1/write"
        }
      }

controller:
  type: deployment
  replicas: 1

clusterRole:
  rules: []
