apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: notifiarr
  namespace: servarr
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: traefik
      sectionName: websecure
  hostnames:
    - "notifiarr.talos-cluster.local.obleey.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: notifiarr
          port: 5454
          weight: 1
